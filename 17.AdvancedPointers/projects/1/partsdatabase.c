#include <stdio.h>

// add_part error if the part is already in the database or if the database is full (insert)
// print_part print the name and current quantity (error if the part number isn't in database) (search)
// change_quantity(int number) error if the part number isn't in the database (update)
// void print_database(struct database *a) print database in the order in which they were entered (print)

